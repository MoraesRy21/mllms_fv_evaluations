[project]
name = "mllms_fv_evaluations"
version = "1.0.0"
description = "Codebase for MLLMs Face Verification Evaluations"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]